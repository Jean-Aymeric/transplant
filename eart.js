"use strict";

const Organ = require("./organ");

/**
 * module Eart
 */
class Eart extends Organ {
    /**
     * @param {String} name
     */
    constructor(name) {
        super(name)
    }
}
module.exports = Eart;
